html
html
   head
      title Form Tester
   body
      form(action = "/", method = "POST")
         div
            label(for = "say") Say:
            input(name = "say" value = "Hi")
         br
         div
            label(for = "to") To:
            input(name = "to" value = "Express forms")
         br
         button(type = "submit") Send my greetings
   -var ob = JSON.parse(json)

   //- ul
   //-    for data in ob.table
   //-       li=data.say + ": " + data.to
   table.table.table-striped
      tr
        th Say
        th To

   each data in ob.table
        tr
          td= data.say
          td= data.to